This folder contains project screenshots used in the main `README.md`.

Place screenshots here (e.g. `home.png`, `case_view.png`) and update the `README.md` image references accordingly.

If you don't yet have images, keep this placeholder file so the folder is tracked by git.
